[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "robust_rl_locomotion"
version = "0.0.0"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
